class: DemandeSupport
table: demandes_support
managedMigration: true

fields:
  utilisateurId: int
  cinemaId: int?        # ← AJOUTER ça
  sujet: String
  message: String
  statut: String?, default='ouvert'
  reponse: String?
  createdAt: DateTime?
  updatedAt: DateTime?

indexes:
  support_utilisateur_idx:
    fields: utilisateurId
  support_cinema_idx: # ← AJOUTER ça
      fields: cinemaId