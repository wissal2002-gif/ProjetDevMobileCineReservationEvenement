class: DemandeSupport
table: demandes_support
fields:
  utilisateurId: int
  sujet: String
  message: String
  statut: String?, default='ouvert'
  reponse: String?
  createdAt: DateTime?
  updatedAt: DateTime?
indexes:
  demande_support_utilisateur_idx:
    fields: utilisateurId