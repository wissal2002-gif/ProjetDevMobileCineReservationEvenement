class: Siege
table: sieges
fields:
  salleId: int
  numero: String
  type: String?, default='standard'
indexes:
  siege_salle_idx:
    fields: salleId
  siege_unique_idx:
    fields: salleId, numero
    unique: true