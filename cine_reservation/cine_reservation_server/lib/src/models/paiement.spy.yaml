class: Paiement
table: paiements
fields:
  reservationId: int
  montant: double
  methode: String?, default='carte'
  statut: String?, default='en_attente'
  stripePaymentId: String?
  createdAt: DateTime?
indexes:
  paiement_reservation_idx:
    fields: reservationId